{
    "extends": "../tsconfig.json",
    // Test comment
    "compilerOptions": {
        "outDir": "../out-tsc/app",
        "types": []
    },
    "exclude": [
        "app/file-to-exclude.ts"
    ]
}
